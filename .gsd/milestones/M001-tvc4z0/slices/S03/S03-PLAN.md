# S03: RuntimeClass Registry

**Goal:** Implement RuntimeClassRegistry: parse config.yaml, ${VAR} substitution, validation
**Demo:** After this: RuntimeClass registry resolves names to launch configs with env substitution

## Tasks

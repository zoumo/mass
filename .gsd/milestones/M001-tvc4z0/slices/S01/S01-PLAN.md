# S01: Scaffolding + Phase 1.3 exitCode

**Goal:** Create agentd package structure, daemon entry point, config parsing; complete Phase 1.3 exitCode propagation for Process Manager dependency
**Demo:** After this: agentd daemon starts with config.yaml, listens on socket; shim exitCode surfaces in GetState

## Tasks

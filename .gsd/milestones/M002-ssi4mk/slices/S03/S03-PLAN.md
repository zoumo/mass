# S03: Recovery and persistence truth-source

**Goal:** Make the runtime's persistence and boundary rules truthful enough to support restart/recovery and safe cleanup on top of the converged contract.
**Demo:** After this: After this: session config, workspace identity, cleanup, and security-boundary rules are explicit enough that restart/recovery and cleanup behavior can be trusted instead of inferred.

## Tasks

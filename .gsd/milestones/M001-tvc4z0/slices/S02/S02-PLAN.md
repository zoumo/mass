# S02: Metadata Store (SQLite)

**Goal:** Implement Metadata Store with SQLite backend: sessions, workspaces, rooms tables with CRUD operations
**Demo:** After this: SQLite metadata store created, CRUD operations work, schema in place

## Tasks

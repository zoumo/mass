# S04: Real CLI integration verification

**Goal:** Prove that the converged contract works against real ACP clients and not only inside rewritten docs or mock-agent tests.
**Demo:** After this: After this: the converged contract is proven against real `gsd-pi` and `claude-code` bundle flows, so M002 ends with live ACP evidence instead of mock-only confidence.

## Tasks
